-- Migration 001: Baseline schema (no-op)
-- Tables are created by initSchema() in src/db/index.js:
-- categories, field_definitions, team_members, locations, asset_models,
-- assets, asset_requests, asset_activity_log, import_batches,
-- label_templates, app_meta
SELECT 1;
